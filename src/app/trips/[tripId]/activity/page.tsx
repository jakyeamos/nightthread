import { Clock3 } from "lucide-react";
import { demoActivity } from "@/lib/demo-data";

export default function ActivityPage() { return <main className="mx-auto max-w-4xl px-6 py-12"><p className="eyebrow">Shared history</p><h1 className="display mt-2 text-5xl">Activity</h1><p className="muted mt-3">A compact record of what changed, without noisy notifications.</p><div className="mt-10">{demoActivity.map(([actor,action,time],index) => <article key={action} className="relative grid grid-cols-[38px_1fr_auto] gap-4 border-b border-[var(--line)] py-5"><span className="grid size-9 place-items-center rounded-full bg-[var(--indigo-soft)] text-xs font-semibold">{actor[0]}</span><div><p className="text-sm"><strong>{actor}</strong> <span className="muted">{action}</span></p></div><time className="muted flex items-center gap-1 text-xs"><Clock3 size={12} />{time}</time>{index < demoActivity.length - 1 && <span className="absolute bottom-[-1px] left-[17px] top-[56px] w-px bg-[var(--line)]" />}</article>)}</div></main>; }
