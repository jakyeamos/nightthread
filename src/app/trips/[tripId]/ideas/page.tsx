/* eslint-disable @next/next/no-img-element -- Unsplash assets must remain hotlinked. */
import { Search, SlidersHorizontal } from "lucide-react";
import { demoIdeas } from "@/lib/demo-data";
import { VoteButton } from "@/components/vote-button";

export default function IdeasPage() { return <main className="mx-auto max-w-6xl px-6 py-12"><div className="flex items-end justify-between"><div><p className="eyebrow">Shared possibility space</p><h1 className="display mt-2 text-5xl">Saved ideas</h1></div><button className="rounded-xl bg-[var(--ink)] px-4 py-3 text-sm font-semibold text-[var(--night)]">Add an idea</button></div><div className="mt-8 flex gap-3"><label className="relative flex-1"><Search className="absolute left-3.5 top-1/2 -translate-y-1/2 text-[var(--faint)]" size={17} /><input placeholder="Search places, categories, cities" className="h-11 w-full rounded-xl border border-[var(--line)] bg-[var(--night-raised)] pl-10 pr-3 text-sm" /></label><button className="surface flex items-center gap-2 rounded-xl px-4 text-sm"><SlidersHorizontal size={15} />Filters</button></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{demoIdeas.map((idea) => <article key={idea.id} className="surface overflow-hidden rounded-2xl"><img src={idea.image} alt="" className="h-44 w-full object-cover" /><div className="p-4"><div className="flex justify-between gap-3"><div><h2 className="font-semibold">{idea.name}</h2><p className="muted mt-1 text-xs">{idea.detail}</p></div><VoteButton initialCount={idea.votes} /></div><p className="mt-5 text-[10px] font-semibold uppercase tracking-wider text-[var(--yarn)]">{idea.priority.replace('_',' ')}</p></div></article>)}</div><p className="muted mt-8 text-xs">Place search powered by Geoapify. Destination imagery includes visible source attribution when imported.</p></main>; }
